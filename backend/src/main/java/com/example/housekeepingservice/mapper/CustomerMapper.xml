<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.housekeepingservice.mapper.CustomerMapper">

    <!-- 插入顾客数据（注册用） -->
    <insert id="insertCustomer" parameterType="com.example.housekeepingservice.entity.Customer01">
        INSERT INTO customer01 (
            username, password, phone, name, gender, create_time, update_time
        ) VALUES (
                     #{username}, #{password}, #{phone}, #{name}, #{gender}, NOW(), NOW()
                 )
    </insert>

    <!-- 根据手机号查询顾客 -->
    <select id="getByPhone" parameterType="String" resultType="com.example.housekeepingservice.entity.Customer01">
        SELECT id, username, password, phone, name, gender
        FROM customer01
        WHERE phone = #{phone}
    </select>

</mapper>